name: sales_app_mobile
description: "Aplikasi Mobile Manajemen Order Sales — Modul Sales"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # HTTP & API
  http: ^1.2.2

  # State Management
  provider: ^6.1.2

  # Secure Storage (untuk refresh token)
  flutter_secure_storage: ^9.2.2

  # Date formatting
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
